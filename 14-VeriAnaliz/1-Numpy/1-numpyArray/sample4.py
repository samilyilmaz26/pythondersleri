import numpy as np
print (np.random.randint(10)) # 0(dahil) ile 10(hariç) arasında random int oluşturur)
print(np.random.randint(1,10,5)) # 1(dahil) ile 10(hariç) arasında 5 değer oluşturarak 5 değer saklayan array oluşturur.)
print(np.random.rand(5)) # 0 ile 1 arasında 5 float değerler oluşturur.)
print(np.random.rand(5)) # 0 ile 1 arasında 5 float değerler oluşturur.